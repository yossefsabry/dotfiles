
#!/usr/bin/env bash

# Install dependencies (adjust to match Arch packaging)
sudo pacman -S aircrack-ng dhcp iptables lighttpd hostapd

# Clone Airgeddon
git clone https://github.com/v1s1t0r1sh3r3/airgeddon.git
cd airgeddon

# Launch it
sudo bash airgeddon.sh

