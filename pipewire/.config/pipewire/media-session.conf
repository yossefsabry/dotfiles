context.properties = {
    default.clock.rate = 48000
    default.clock.quantum = 1024
    default.clock.min-quantum = 16
    default.clock.target-quantum = 576
}

# Set the default input device to your AirPods mic
node.default_audio.input = "alsa_input.pci-0000_00_1b.0.analog-stereo"  # Replace with actual AirPods mic name

